# 翻译功能故障诊断报告

## 📋 问题概述

**故障现象**: 用户输入英语单词并点击"一键解析"按钮时，系统弹出"Translation failed"错误提示

**错误信息**: Connection error

**影响范围**: 所有翻译功能完全不可用

***

## 🔍 排查过程

### 1. 架构审查 ✅

**LLM 池机制分析**:

- 项目使用 [`llmPool.ts`](file:///Users/elee987/Downloads/web_compressed/src/lib/llmPool.ts) 实现多提供商故障转移
- 支持多个 LLM API 提供商，按优先级自动切换
- 当某个提供商配额用尽时，自动切换到下一个可用提供商
- 错误检测机制包括：
  - `isQuotaError()`: 检测配额错误（402、429 状态码）
  - `isRateLimitError()`: 检测请求限制
  - `isConnectionError()`: 检测连接错误

**配置参数**:

- 数据库表：`LlmApiProvider`
- 字段：`priority`（优先级）、`quotaRemaining`（剩余额度）、`isActive`（是否启用）
- 当前配置了 2 个提供商：
  1. 火山方舟（默认）- 优先级：0，额度：∞
  2. ds32 - 优先级：0，额度：∞

### 2. Git 提交历史分析 ✅

**最近提交**:

- `9fdba3e` (最新): "增加了错误提示" - 增强了错误处理逻辑，添加了更详细的错误分类
- `abf2ce1`: "在创建密钥分享功能过程中发现翻译 bug 准备修复"

**关键变更**:

- 最新提交中增强了 `isConnectionError()` 函数，添加了更多连接错误模式检测
- 添加了 SSL、证书、DNS 等错误模式的识别
- 在 `withLlmFailover()` 中添加了详细的日志输出

### 3. API 连通性测试 ✅

**测试结果**:

| 测试方法          | 结果 | 错误信息                          |
| ------------- | -- | ----------------------------- |
| curl 直接调用     | ❌  | SetLimitExceeded（配额用尽）        |
| Node.js fetch | ❌  | fetch failed                  |
| Node.js https | ❌  | SELF\_SIGNED\_CERT\_IN\_CHAIN |
| OpenAI SDK    | ❌  | Connection error              |

**关键发现**:

- curl 可以成功连接到 API，但返回配额用尽错误
- Node.js 环境完全无法连接，报 SSL 证书错误

### 4. 根本原因定位 ✅

**问题 1: SSL 证书验证失败** 🔴

```
错误：self-signed certificate in certificate chain
错误代码：SELF_SIGNED_CERT_IN_CHAIN
```

**原因分析**:

- 火山方舟 API 服务器 (`ark.cn-beijing.volces.com`) 使用的是**自签名证书**
- Node.js 默认不信任自签名证书
- curl 使用系统钥匙串，可以验证证书
- Node.js 运行时使用自己的证书存储，不信任该证书

**问题 2: 模型配额用尽** ⚠️

```json
{
  "error": {
    "code": "SetLimitExceeded",
    "message": "Your account has reached the set inference limit for the deepseek-v3 model"
  }
}
```

**原因分析**:

- 账户的 deepseek-v3 模型推理限额已用尽
- 模型服务已被暂停
- 需要调整"安全体验模式"设置

***

## 💡 修复建议

### 方案 1: 禁用 Node.js SSL 验证（快速修复，不推荐生产环境）

**适用场景**: 开发/测试环境

**步骤**:

1. 在 [`src/lib/connectionPool.ts`](file:///Users/elee987/Downloads/web_compressed/src/lib/connectionPool.ts) 中修改 OpenAI 客户端创建逻辑：

```typescript
import OpenAI from 'openai';
import https from 'https';

class ConnectionPool {
  private pool: Map<string, OpenAI> = new Map();
  private maxConnections = 10;

  getClient(apiKey: string, baseUrl: string): OpenAI {
    const key = `${apiKey}:${baseUrl}`;
    if (this.pool.has(key)) {
      return this.pool.get(key)!;
    }

    if (this.pool.size >= this.maxConnections) {
      const oldestKey = this.pool.keys().next().value;
      if (oldestKey) {
        this.pool.delete(oldestKey);
      }
    }

    // 创建自定义 HTTPS Agent，禁用证书验证
    const httpsAgent = new https.Agent({
      rejectUnauthorized: false,
    });

    const client = new OpenAI({ 
      apiKey, 
      baseURL: baseUrl,
      httpAgent: httpsAgent,
    });
    this.pool.set(key, client);
    return client;
  }

  clear() {
    this.pool.clear();
  }
}

export const connectionPool = new ConnectionPool();
```

1. 或者在应用启动时设置环境变量（不推荐）：

```bash
export NODE_TLS_REJECT_UNAUTHORIZED=0
```

**风险**: ⚠️ 禁用 SSL 验证会使应用容易受到中间人攻击

***

### 方案 2: 添加证书到信任列表（推荐）

**步骤**:

1. 获取火山方舟 API 服务器的证书：

```bash
openssl s_client -connect ark.cn-beijing.volces.com:443 -showcerts > volces.crt
```

1. 在代码中显式信任该证书：

```typescript
import OpenAI from 'openai';
import https from 'https';
import fs from 'fs';

const volcesCA = fs.readFileSync('/path/to/volces.crt');

const httpsAgent = new https.Agent({
  ca: volcesCA,
  rejectUnauthorized: true,
});

const client = new OpenAI({ 
  apiKey, 
  baseURL: baseUrl,
  httpAgent: httpsAgent,
});
```

***

###

### 方案 5: 增强错误处理和用户提示

**修改** **[`src/app/api/translate/route.ts`](file:///Users/elee987/Downloads/web_compressed/src/app/api/translate/route.ts)**:

```typescript
} catch (error: any) {
  console.error('API Error:', error);
  
  // 检查配额用尽
  if (String(error?.message || '') === API_QUOTA_EXHAUSTED_MESSAGE) {
    return NextResponse.json({ 
      error: API_QUOTA_EXHAUSTED_MESSAGE 
    }, { status: 503 });
  }
  
  // 检查 SSL 证书错误
  if (error?.code === 'SELF_SIGNED_CERT_IN_CHAIN' || 
      error?.message?.includes('certificate')) {
    return NextResponse.json({ 
      error: 'SSL 证书验证失败，请联系管理员检查证书配置',
      details: error?.message
    }, { status: 503 });
  }
  
  // 检查连接错误
  if (/connection|timeout|network|ECONNREFUSED|ENOTFOUND/i.test(error?.message || '')) {
    return NextResponse.json({ 
      error: '无法连接到翻译服务，请检查网络连接',
      details: error?.message
    }, { status: 503 });
  }
  
  // 检查配额限制
  if (error?.message?.includes('SetLimitExceeded') || 
      error?.message?.includes('inference limit')) {
    return NextResponse.json({ 
      error: '模型配额已用尽，请联系管理员调整配额或切换模型',
      details: error?.message
    }, { status: 503 });
  }
  
  return NextResponse.json({ 
    error: 'Translation failed',
    details: error?.message
  }, { status: 500 });
}
```

***

## 📝 测试验证

修复后请运行以下测试：

```bash
# 1. 运行 LLM 连接测试脚本
npx tsx test-llm-connection.ts

# 2. 使用测试账号登录
# 账号：creator
# 密码：creator987

# 3. 访问翻译页面测试功能
http://localhost:3000/translate

# 4. 检查浏览器控制台和服务器日志
```

***

## 🔗 相关文件

- LLM 池实现：[`src/lib/llmPool.ts`](file:///Users/elee987/Downloads/web_compressed/src/lib/llmPool.ts)
- 连接池管理：[`src/lib/connectionPool.ts`](file:///Users/elee987/Downloads/web_compressed/src/lib/connectionPool.ts)
- 翻译 API：[`src/app/api/translate/route.ts`](file:///Users/elee987/Downloads/web_compressed/src/app/api/translate/route.ts)
- LLM 配置页面：[`src/app/llm-config/page.tsx`](file:///Users/elee987/Downloads/web_compressed/src/app/llm-config/page.tsx)
- 环境变量：`.env`

***

## 📊 故障时间线

1. **未知时间**: 火山方舟 API 配额用尽，模型服务被暂停
2. **用户使用时**: 点击"一键解析"按钮
3. **系统响应**: 尝试连接 LLM API
4. **第一次失败**: SSL 证书验证失败（Node.js 环境）
5. **错误传播**: `withLlmFailover` 捕获错误，标记为连接错误
6. **切换机制**: 尝试切换到其他提供商（但所有提供商都有相同的 SSL 问题）
7. **最终错误**: 所有提供商失败，返回"Translation failed"

***

## ✅ 结论

**故障根本原因**:

1. **主要问题**: Node.js 环境不信任火山方舟 API 服务器的自签名证书

**影响**: 所有翻译功能完全不可用

**修复优先级**:

1. 临时禁用 SSL 验证（立即）
2. 调整配额设置（立即）
3. 正确配置证书信任（短期）
4. 增强错误提示（短期）

**预计修复时间**: 30 分钟 - 2 小时

***

**报告生成时间**: 2026-04-13\
**诊断工具**: 自定义测试脚本、curl、Node.js 网络诊断
