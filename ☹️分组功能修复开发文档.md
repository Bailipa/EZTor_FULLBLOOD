# 分组功能修复开发文档

## 1. 问题概述

在部署环境（https://dogeggcode.cyou）中，分组功能无法正常使用，报错"invalid origin"。

## 2. 问题原因

### 2.1 CSRF检查逻辑不一致
- `/api/review-groups/[id]/words/route.ts` 使用了自定义的 `checkCSRF` 函数
- 该函数只允许本地开发环境的origin和 `process.env.NEXT_PUBLIC_APP_URL`
- 部署环境的域名 `https://dogeggcode.cyou` 不在允许列表中

### 2.2 与其他API的CSRF检查不一致
- 其他API（如 `/api/sync`）使用的是 `/src/lib/csrf.ts` 中的 `checkCsrfHeader` 函数
- `checkCsrfHeader` 会根据请求的 `host` 头动态生成允许的origin列表

## 3. 解决方案

### 3.1 推荐方案：统一CSRF检查逻辑
- 将 `/api/review-groups/[id]/words/route.ts` 中的 `checkCSRF` 函数替换为使用 `checkCsrfHeader`
- 这样可以与其他API保持一致，自动适应不同的部署环境

### 3.2 备选方案
1. **修改checkCSRF函数**：在 `checkCSRF` 函数的 `allowedOrigins` 数组中添加部署环境的域名
2. **配置环境变量**：确保 `NEXT_PUBLIC_APP_URL` 环境变量正确设置为 `https://dogeggcode.cyou`

## 4. 实施步骤

### 4.1 步骤1：修改API文件

**文件**：`/src/app/api/review-groups/[id]/words/route.ts`

**修改前**：
```typescript
function checkCSRF(req: Request): boolean {
  const origin = req.headers.get('origin') || req.headers.get('referer');
  if (!origin) return true; // Allow requests without origin (e.g., from the same origin)
  
  const allowedOrigins = [
    'http://localhost:3000',
    'http://localhost:3001',
    'http://127.0.0.1:3000',
    'http://127.0.0.1:3001',
    process.env.NEXT_PUBLIC_APP_URL,
  ].filter(Boolean);
  
  return allowedOrigins.some(allowed => origin.startsWith(allowed || ''));
}

export async function POST(req: Request, { params }: { params: Promise<{ id: string }> }) {
  try {
    if (!checkCSRF(req)) {
      return NextResponse.json({ success: false, error: 'Invalid origin' }, { status: 403 });
    }
    // ... 其他代码
  }
}
```

**修改后**：
```typescript
import { checkCsrfHeader } from '@/lib/csrf';

export async function POST(req: Request, { params }: { params: Promise<{ id: string }> }) {
  try {
    const csrf = checkCsrfHeader(req);
    if (!csrf.valid) {
      return NextResponse.json({ success: false, error: csrf.reason || 'Invalid origin' }, { status: 403 });
    }
    // ... 其他代码
  }
}
```

### 4.2 步骤2：更新其他HTTP方法

同样修改 `DELETE` 和 `GET` 方法中的CSRF检查：

**修改前**：
```typescript
export async function DELETE(req: Request, { params }: { params: Promise<{ id: string }> }) {
  try {
    if (!checkCSRF(req)) {
      return NextResponse.json({ success: false, error: 'Invalid origin' }, { status: 403 });
    }
    // ... 其他代码
  }
}
```

**修改后**：
```typescript
export async function DELETE(req: Request, { params }: { params: Promise<{ id: string }> }) {
  try {
    const csrf = checkCsrfHeader(req);
    if (!csrf.valid) {
      return NextResponse.json({ success: false, error: csrf.reason || 'Invalid origin' }, { status: 403 });
    }
    // ... 其他代码
  }
}
```

### 4.3 步骤3：环境配置

确保生产环境中设置了正确的环境变量：

```bash
# .env.production 文件
NEXT_PUBLIC_APP_URL=https://dogeggcode.cyou
```

## 5. 代码审查要点

### 5.1 安全性
- 确保CSRF检查逻辑正确实现
- 验证 `checkCsrfHeader` 函数能够正确处理不同环境的origin
- 确保所有HTTP方法都有适当的CSRF保护

### 5.2 一致性
- 检查其他API路由是否也使用了 `checkCsrfHeader` 函数
- 确保错误响应格式与其他API保持一致

### 5.3 错误处理
- 确保CSRF检查失败时返回清晰的错误信息
- 检查是否有适当的日志记录

## 6. 测试计划

### 6.1 本地测试
1. 启动开发服务器
2. 登录系统
3. 测试分组功能：
   - 创建新分组
   - 添加单词到分组
   - 从分组中删除单词
   - 清空分组

### 6.2 部署环境测试
1. 部署修复后的代码到生产环境
2. 登录 https://dogeggcode.cyou
3. 测试分组功能的完整流程：
   - 进入"生词本"页面
   - 点击"管理分组"
   - 选取生词本中的单词
   - 点击"加入分组"
   - 创建新分组或选择已有分组，点击"确定添加"按钮
   - 验证单词成功添加到分组
   - 验证分组的单词计数更新

### 6.3 边界情况测试
1. 测试不同浏览器的兼容性
2. 测试不同网络环境下的表现
3. 测试未设置 `NEXT_PUBLIC_APP_URL` 环境变量的情况

## 7. 风险评估

### 7.1 潜在风险
- **部署风险**：如果 `checkCsrfHeader` 函数在部署环境中表现异常，可能导致所有API请求失败
- **兼容性风险**：修改CSRF检查逻辑可能影响其他依赖该API的功能
- **安全风险**：如果CSRF检查过于宽松，可能引入安全漏洞

### 7.2 缓解措施
- 在修改前进行充分的测试
- 确保 `checkCsrfHeader` 函数的实现正确
- 监控部署后的系统表现

## 8. 预期效果

### 8.1 功能恢复
- 分组功能在部署环境中正常工作
- 用户可以成功将单词添加到指定分组
- 分组的单词计数正确更新

### 8.2 架构改进
- 统一了CSRF检查逻辑
- 提高了系统的可维护性
- 减少了部署配置的复杂性

## 9. 文档更新

### 9.1 技术文档
- 更新API文档，说明统一的CSRF检查机制
- 更新部署文档，说明环境变量的配置要求

### 9.2 用户文档
- 更新用户手册，说明分组功能的使用方法
- 添加常见问题解答，解释可能的错误情况

## 10. 结论

通过统一CSRF检查逻辑，可以解决部署环境中分组功能失败的问题，同时提高项目的整体架构一致性和可维护性。建议采用推荐方案，这样可以从根本上解决问题，同时避免未来类似问题的发生。