# 当然功能开发文档

## 1. 功能实现要点

### 1.1 前进后退按键
- **实现位置**：`src/components/ui/flashcard/flashcard-widget.tsx`
- **核心功能**：
  - 在闪卡界面顶部添加两个按钮组件
  - 实现`handlePrevious`函数，用于切换到上一个单词
  - 修改现有`handleNext`函数，使其在非答题模式下也能切换单词
  - 添加边界检查逻辑，控制按钮禁用状态

### 1.2 生词本操作优化
- **实现位置**：`src/components/ui/flashcard/flashcard-widget.tsx`
- **核心功能**：
  - 移动"加入生词本"按钮位置，与其他操作按钮并列
  - 实现单词是否在生词本中的检查逻辑
  - 根据检查结果显示相应的按钮状态
  - 移除alert提示，改为视觉反馈

### 1.3 不再出现功能
- **实现位置**：`src/components/ui/flashcard/flashcard-widget.tsx`
- **核心功能**：
  - 添加"不再出现"按钮，与"再出现"按钮共用同一位置
  - 实现条件渲染逻辑，当答案显示时显示此按钮
  - 实现`handleIgnoreWord`函数，调用API标记单词为忽略
  - 标记后自动切换到下一个单词

### 1.4 再出现功能
- **实现位置**：
  - `src/components/ui/flashcard/flashcard-widget.tsx`（添加入口按钮）
  - 新增文件 `src/components/ui/flashcard/ignored-words.tsx`（实现忽略单词列表界面）
- **核心功能**：
  - 添加"再出现"按钮，与"不再出现"按钮共用同一位置
  - 实现条件渲染逻辑，当答案未显示时显示此按钮
  - 创建新的对话框组件，显示被忽略的单词列表
  - 实现API调用，获取被忽略的单词列表
  - 实现单词恢复功能，点击后将单词从忽略列表中移除

## 2. 技术选型依据

### 2.1 前端技术栈
- **React**：使用函数组件和Hooks进行状态管理
- **Next.js**：项目基础框架
- **Tailwind CSS**：样式管理
- **Lucide React**：图标库
- **shadcn/ui**：UI组件库（Button、Card、Dialog等）

### 2.2 数据管理
- **状态管理**：使用React useState管理组件内部状态
- **API调用**：使用fetch API进行网络请求
- **会话管理**：使用next-auth的useSession钩子

## 3. 核心模块设计说明

### 3.1 FlashcardWidget组件
- **状态管理**：
  - `words`：单词列表
  - `currentIndex`：当前单词索引
  - `showAnswer`：是否显示答案
  - `isLoading`：加载状态
  - `isOpen`：对话框是否打开
  - `isSaving`：保存状态
  - `groups`：复习组列表
  - `selectedGroupId`：当前选择的复习组
  - `isUpdating`：更新状态

- **核心函数**：
  - `fetchGroups`：获取复习组列表
  - `fetchWords`：获取单词列表
  - `handleGroupChange`：处理复习组变化
  - `handleSaveToPrivate`：保存单词到生词本
  - `handleNext`：处理下一个单词
  - `handlePrevious`：处理上一个单词（新增）
  - `handleIgnoreWord`：处理单词忽略（新增）
  - `playAudio`：播放单词发音

### 3.2 IgnoredWords组件（新增）
- **状态管理**：
  - `ignoredWords`：被忽略的单词列表
  - `isLoading`：加载状态
  - `isRestoring`：恢复状态

- **核心函数**：
  - `fetchIgnoredWords`：获取被忽略的单词列表
  - `handleRestoreWord`：恢复被忽略的单词

## 4. 接口定义规范

### 4.1 现有接口
- **GET /api/review-groups**：获取复习组列表
- **GET /api/flashcard/public**：获取公共词库单词
- **POST /api/dictation/update**：更新单词状态

### 4.2 新增接口
- **POST /api/flashcard/ignore**：标记单词为忽略
  - 参数：`{ word: string }`
  - 返回：`{ success: boolean, message: string }`

- **GET /api/flashcard/ignored**：获取被忽略的单词列表
  - 参数：无
  - 返回：`{ success: boolean, data: Word[] }`

- **POST /api/flashcard/restore**：恢复被忽略的单词
  - 参数：`{ word: string }`
  - 返回：`{ success: boolean, message: string }`

## 5. 与现有代码架构的集成方案

### 5.1 FlashcardWidget组件修改
1. **添加前进后退按钮**：
   - 在对话框标题区域添加两个Button组件
   - 实现`handlePrevious`函数
   - 修改`handleNext`函数，添加非答题模式的处理

2. **调整生词本按钮**：
   - 移动按钮位置到操作区域
   - 添加单词是否在生词本中的检查逻辑
   - 修改按钮状态显示
   - 移除alert提示，改为视觉反馈

3. **添加不再出现/再出现按钮**：
   - 添加条件渲染逻辑，根据`showAnswer`状态显示不同按钮
   - 实现`handleIgnoreWord`函数
   - 添加"再出现"按钮的点击事件，打开IgnoredWords对话框

### 5.2 IgnoredWords组件创建
1. **组件结构**：
   - 使用Dialog组件创建对话框
   - 使用Card组件显示被忽略的单词
   - 使用Button组件实现恢复功能

2. **数据流程**：
   - 组件挂载时获取被忽略的单词列表
   - 点击恢复按钮时调用API恢复单词
   - 恢复成功后更新列表

### 5.3 API接口实现
1. **后端路由**：
   - 在`pages/api/flashcard/`目录下创建`ignore.ts`、`ignored.ts`、`restore.ts`文件

2. **数据存储**：
   - 使用数据库存储用户忽略的单词
   - 在用户表中添加忽略单词的关联

3. **权限控制**：
   - 确保只有登录用户才能忽略和恢复单词

## 6. 实现注意事项

### 6.1 性能优化
- **单词列表加载**：使用缓存机制减少重复请求
- **被忽略单词列表**：实现分页加载，避免一次性加载过多数据

### 6.2 边界情况处理
- **单词列表为空**：显示友好的提示信息
- **只有一个单词**：禁用前进后退按钮
- **未登录用户**：禁用需要登录的功能，显示登录提示

### 6.3 状态同步
- **生词本状态**：确保单词是否在生词本中的状态及时更新
- **忽略状态**：确保被忽略的单词不会出现在单词列表中

### 6.4 用户体验
- **加载状态**：添加适当的加载指示器
- **错误处理**：显示友好的错误提示
- **动画效果**：添加适当的过渡动画，提升用户体验

## 7. 开发计划

1. **第一阶段**：实现前进后退按键功能
2. **第二阶段**：优化生词本操作功能
3. **第三阶段**：实现不再出现功能
4. **第四阶段**：实现再出现功能
5. **第五阶段**：测试和优化

## 8. 代码架构图

```
┌─────────────────────────────────────────────┐
│ FlashcardWidget                             │
├─────────────────────────────────────────────┤
│ ┌─────────────┐  ┌─────────────┐  ┌─────────┐ │
│ │ 前进/后退按钮 │  │ 生词本操作  │  │ 再出现/ │ │
│ │             │  │             │  │ 不再出现 │ │
│ └─────────────┘  └─────────────┘  └─────────┘ │
│                                               │
│ ┌───────────────────────────────────────────┐ │
│ │ 单词显示区域                              │ │
│ │                                           │ │
│ │ ┌───────────────────────────────────────┐ │ │
│ │ │ 单词、音标、发音按钮                   │ │ │
│ │ └───────────────────────────────────────┘ │ │
│ │                                           │ │
│ │ ┌───────────────────────────────────────┐ │ │
│ │ │ 答案显示（条件渲染）                  │ │ │
│ │ └───────────────────────────────────────┘ │ │
│ └───────────────────────────────────────────┘ │
│                                               │
│ ┌───────────────────────────────────────────┐ │
│ │ 操作按钮区域                              │ │
│ │                                           │ │
│ │ ┌─────────────┐  ┌─────────────┐          │ │
│ │ │ 不认识按钮  │  │ 认识按钮    │          │ │
│ │ └─────────────┘  └─────────────┘          │ │
│ └───────────────────────────────────────────┘ │
└─────────────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────┐
│ IgnoredWords                                │
├─────────────────────────────────────────────┤
│ ┌───────────────────────────────────────────┐ │
│ │ 被忽略单词列表                            │ │
│ │                                           │ │
│ │ ┌───────────────────────────────────────┐ │ │
│ │ │ 单词卡片                              │ │ │
│ │ │ 单词、音标、释义                      │ │ │
│ │ │ 恢复按钮                              │ │ │
│ │ └───────────────────────────────────────┘ │ │
│ │                                           │ │
│ │ ┌───────────────────────────────────────┐ │ │
│ │ │ 单词卡片                              │ │ │
│ │ │ 单词、音标、释义                      │ │ │
│ │ │ 恢复按钮                              │ │ │
│ │ └───────────────────────────────────────┘ │ │
│ └───────────────────────────────────────────┘ │
└─────────────────────────────────────────────┘
```

## 10. 核心函数实现示例

### 10.1 handlePrevious函数
```typescript
const handlePrevious = () => {
  if (currentIndex > 0) {
    setCurrentIndex(prev => prev - 1);
    setShowAnswer(false);
  }
};
```

### 10.2 handleIgnoreWord函数
```typescript
const handleIgnoreWord = async () => {
  if (!currentWord) return;
  
  if (status !== 'authenticated' || !session?.user) {
    alert("请先登录后再标记单词");
    return;
  }
  
  try {
    const res = await fetch('/api/flashcard/ignore', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ word: currentWord.word })
    });
    
    const data = await res.json();
    
    if (!res.ok) {
      if (res.status === 401) {
        alert("请先登录后再标记单词");
      } else {
        throw new Error(data.error || '标记失败');
      }
      return;
    }
    
    if (data.success) {
      // 标记成功，切换到下一个单词
      if (currentIndex < words.length - 1) {
        setCurrentIndex(prev => prev + 1);
        setShowAnswer(false);
      } else {
        fetchWords();
      }
    } else {
      throw new Error(data.error || '标记失败');
    }
  } catch (e: any) {
    console.error("Failed to ignore word:", e);
    alert(e.message || "标记失败，请稍后重试");
  }
};
```

## 11. 总结

本开发文档基于需求文稿，详细说明了"当然"功能的技术实现方案。通过合理的模块设计和接口定义，确保功能的实现与现有代码架构高度集成。开发过程中需要注意性能优化、边界情况处理、状态同步和用户体验，以确保功能的稳定性和流畅性。