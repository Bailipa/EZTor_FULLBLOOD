
现在不知为何，翻译解析多个单词（用分行分开）时，报错fail to fetch！

然后要删去csv文件导入功能（只删除前端按钮），因为我实在想不到这个需求是什么，删了更好。

LLM池的API设计必须让所有的大模型厂商的大模型API接口都能用

数据隔离是否有效？一个用户对自己的词汇增删会不会影响另外的用户？，算法复杂度和使用体验是否最优（是否卡顿，进度条位置是否起到提醒作用）

说实话我不喜欢用户之间相互联系，要不把分享功能砍掉（只要把UI去掉就行了），只保留四六级密钥导入的功能，然后把密钥导入的UI，标题什么的改一下吧，这样也省心一点。

translate only还得加限制，每个用户每日10次免费使用，超出限制放出API填写框，让用户自己接大模型，就不用消耗我的额度了

必须加一个捐款链接，捐款链接用后端数据库来对接png图像，请我喝咖啡(开小号必须快点了)

补齐词库中的例句

内存管理，防止长期内存泄露

最后：架构审查，部署审查
