# 默认词库导入使用指南

## 问题说明

如果你发现点击推荐的"大学英语四六级核心词汇"时提示"密钥格式无效"，这是因为：

**原因**：默认词库组件中显示的密钥格式与实际数据库中的密钥格式不匹配。

- ❌ **旧格式**（错误）：`CET4-6-2026-VOCAB`（长格式）
- ✅ **新格式**（正确）：`M5P-VPF-WQV`（3-3-3 短格式）

## 解决方案

### 方案一：使用更新后的推荐词库（推荐）

现在组件已更新，点击推荐的词库会自动填充正确的密钥：

| 词库名称 | 密钥 | 词汇数 | 状态 |
|---------|------|--------|------|
| 大学英语四六级核心词汇 | `M5P-VPF-WQV` | 4,539 | ✅ 已导入 |
| 雅思核心词汇 | `G24-9PL-FH3` | 0 | ⏳ 待导入 |
| 大学英语六级核心词汇 | `GU6-AXY-WK7` | 2,118 | ✅ 已导入 |
| 考研核心词汇 | `RHC-NRA-6UR` | 0 | ⏳ 待导入 |

### 方案二：手动输入密钥

在导入页面手动输入以上密钥即可。

## 使用步骤

### 1. 访问导入页面

进入生词本页面（`/history`），点击"密钥导入"按钮。

### 2. 选择推荐词库

点击任意推荐的词库卡片，系统会自动填充密钥。

### 3. 设置分组名称

输入你想要的分组名称，例如"四级词汇"。

### 4. 开始导入

点击"导入"按钮，等待导入完成。

## 密钥格式说明

### 正确格式（3-3-3）

```
格式：XXX-XXX-XXX
示例：M5P-VPF-WQV

特点：
- 3 段，每段 3 个字符
- 只包含大写字母 A-Z（排除 0, O, 1, I, l）
- 只包含数字 2-9
- 段之间用连字符 - 分隔
- 总长度 11 个字符
```

### 错误格式示例

```
❌ CET4-6-2026-VOCAB（太长，不是 3-3-3 格式）
❌ ABC-123-XYZ（包含被排除的字符）
❌ abc-def-ghi（小写字母）
❌ ABC123XYZ（缺少连字符）
```

## 如何导入完整词库

目前默认词库的词汇数为 0，需要手动导入完整数据：

### 步骤 1：克隆词汇数据仓库

```bash
git clone https://github.com/KyleBing/english-vocabulary.git /tmp/english-vocabulary
```

### 步骤 2：运行导入脚本

```bash
# 导入四级词汇
npx tsx scripts/import-vocabulary-from-github.ts cet4

# 导入六级词汇
npx tsx scripts/import-vocabulary-from-github.ts cet6

# 导入考研词汇
npx tsx scripts/import-vocabulary-from-github.ts postgraduate

# 导入所有词库
npx tsx scripts/import-vocabulary-from-github.ts all
```

### 步骤 3：验证导入结果

导入完成后，刷新导入页面，词库的词汇数会更新。

## 常见问题

### Q: 为什么点击推荐词库提示"密钥格式无效"？

A: 这是因为组件中显示的密钥格式与实际不匹配。现在已修复，点击推荐词库会自动填充正确的 3-3-3 格式密钥。

### Q: 导入后词汇数为 0 怎么办？

A: 默认词库只创建了分组和密钥，需要手动导入词汇数据。请参考"如何导入完整词库"部分。

### Q: 可以自定义词库吗？

A: 可以。你可以：
1. 创建自己的 ReviewGroup
2. 生成分享密钥
3. 导入词汇数据
4. 分享给其他人使用

### Q: 导入失败怎么办？

A: 检查以下几点：
1. 密钥格式是否正确（3-3-3 格式）
2. 密钥是否有效（未过期、未失效）
3. 是否已重复导入
4. 网络连接是否正常

## 相关文档

- [GitHub 词汇数据导入指南](./GITHUB_VOCABULARY_IMPORT.md)
- [默认词库配置文档](./DEFAULT_VOCABULARY_SETUP.md)
- [分享功能 API 文档](./API_SHARE_DEFAULTS.md)

---

**更新日期**: 2026-04-13  
**版本**: 1.1
