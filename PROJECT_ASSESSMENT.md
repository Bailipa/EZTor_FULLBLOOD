# EZTor 项目健壮性与可部署性评估报告

> **评估日期**：2026-04-29  
> **项目版本**：0.3.0  
> **项目类型**：Next.js 16 App Router 全栈 Web 应用  
> **技术栈**：Next.js 16.2.3 + React 19 + TypeScript 5 + Prisma 5 + SQLite + Tailwind CSS 4 + Vitest

---

## 目录

1. [项目健壮性评估](#一项目健壮性评估)
2. [项目可部署性评估](#二项目可部署性评估)
3. [主要风险点识别](#三主要风险点识别)
4. [改进建议优先级排序](#四改进建议优先级排序)
5. [总体评估结论](#五总体评估结论)

---

## 一、项目健壮性评估

### 1.1 错误处理机制

**评分：★★★★☆（良好）**

项目实现了**多层纵深防御**的错误处理架构，从启动阶段到运行时覆盖完整链路：

| 层级 | 机制 | 实现位置 | 评估 |
|------|------|----------|------|
| 启动前置验证 | Fail-Fast 环境校验，生产环境无效直接 `process.exit(1)` | `src/instrumentation.ts` + `envValidator.ts` | ✅ 优秀 |
| 请求边界守卫 | 公开路径放行 → CSRF → 认证 → 权限（分层链式守卫） | `src/middleware.ts` | ✅ 良好 |
| API 错误处理 | 错误码映射表（Prisma + 域名）+ 语义匹配 + 通用兜底 | `src/lib/apiErrorHandler.ts` | ✅ 良好 |
| 组件级错误边界 | React Error Boundary（重试/刷新页面恢复） | `src/components/error-boundary.tsx` | ✅ 良好 |
| 路由级错误边界 | Next.js error.tsx（重试/返回首页） | `src/app/error.tsx` 及各子路由 | ✅ 良好 |
| 加载态优雅降级 | 骨架屏（Skeleton）保持视觉连续性 | `src/app/loading.tsx` 及各子路由 | ✅ 良好 |

**关键亮点**：
- **环境感知降级**：开发环境返回详细错误栈，生产环境返回通用中文提示，防止敏感信息泄露
- **API/页面差异化处理**：API 路由返回 JSON 错误响应，页面路由执行友好重定向，避免 fetch 跟随 307 重定向导致 405 错误
- **统一响应格式**：所有 API 响应遵循 `{ success: boolean, error: string }` 结构

**待改进项**：
- `banManager.ts` 中多处 `catch {}` 静默吞异常，关键路径（如违规记录写入失败）应向上抛出而非静默失败
- `requestDeduplication.ts` 容量上限达 1000 后静默返回 `false`，调用方可能未处理此边界

---

### 1.2 边界条件处理

**评分：★★★☆☆（中等）**

| 场景 | 处理方式 | 评估 |
|------|----------|------|
| 空值/未定义输入 | `security.ts` 中 `validateInput` 做空值检查与长度限制 | ✅ 已覆盖 |
| 超长输入 | 普通输入 2000 字符、翻译输入 8000 字符截断 | ✅ 已覆盖 |
| 大量词表导入 | 每词限 500 字符、总词数限 100 个 | ✅ 已覆盖 |
| 并发冲突 | 分享导入的 `maxUses` 原子性检查（乐观锁） | ✅ 已处理 |
| 数据库连接池 | 自定义 `ConnectionPool` 模块管理 | ✅ 已处理 |
| LLM 连接池 | 多提供商 `LlmPool` 支持优先级+配额 | ✅ 已处理 |
| IP 封禁边界 | 支持代理头（`x-forwarded-for`、`x-real-ip`） | ✅ 已处理 |

**待改进项**：
- 缺少 Unicode 规范化（NFKC），攻击者可能利用同形异意字符绕过输入检测
- 固定窗口速率限制存在边缘突发问题（窗口边界瞬间可达 2× 请求量）

---

### 1.3 数据验证

**评分：★★★☆☆（中等）**

项目在多个层次实施了数据验证：

| 验证层 | 机制 | 评估 |
|--------|------|------|
| 环境变量 | 启动时校验完整性（必需/可选）、安全性（长度/强度）、默认值检测 | ✅ 优秀 |
| 用户输入 | 净化 LLM 控制标记、转义特殊字符、长度截断 | ✅ 良好 |
| AI 输出 | 验证 LLM 返回结果的结构完整性（word/translation 必填字段） | ✅ 良好 |
| 数据库唯一性 | 44 个索引覆盖关键查询与唯一约束 | ✅ 优秀 |
| 分享码格式 | 格式校验 + 长度校验 + 模糊字符排除（0/O/1/I/l） | ✅ 良好 |

**量化指标**：
- Prisma Schema 定义 18 个数据模型
- 44 个数据库索引（含唯一索引），平均每模型 2.4 个
- 9 次数据库迁移，历史演进清晰

**待改进项**：
- `validateAiOutput` 仅校验 `word` 和 `translation` 字段，未对 `phonetic`、`pos` 等辅助字段做注入检测
- 输入净化采用静默修改策略（移除恶意内容），用户可能不知道输入被篡改

---

### 1.4 异常恢复能力

**评分：★★★☆☆（中等）**

| 恢复场景 | 机制 | 评估 |
|----------|------|------|
| 应用崩溃 | PM2 `autorestart: true`，内存超 1GB 自动重启 | ✅ 已配置 |
| 组件渲染错误 | Error Boundary 重试按钮（重置状态）+ 刷新页面 | ✅ 已配置 |
| 数据库操作失败 | Prisma 事务回滚（分享导入流程中已验证） | ✅ 已验证 |
| 环境配置错误 | 生产环境 Fail-Fast，开发环境仅警告不阻断 | ✅ 合理 |
| IP 封禁到期 | `checkUserBan` 自动解封过期封禁，无需定时任务 | ✅ 设计巧妙 |
| 数据损坏 | 提供 SQLite 二进制/SQL 双格式备份 + 恢复文档 | ✅ 已准备 |

**待改进项**：
- 无健康检查端点（health check），负载均衡器无法判断实例是否健康
- 纯内存的 `rateLimit` 和 `requestDeduplication` 在多实例重启后状态全部丢失
- 缺少优雅关闭（graceful shutdown）处理：无 `SIGTERM` 信号监听，无连接排空逻辑

---

### 1.5 代码质量与测试覆盖率

**评分：★★☆☆☆（需改进）**

#### 代码规范

| 检查项 | 结果 | 状态 |
|--------|------|------|
| TypeScript 严格模式 | `tsconfig.json` 中 `strict: true` | ✅ 已启用 |
| ESLint 配置 | 基于 `eslint-config-next` 扁平配置 | ✅ 已配置 |
| 编辑器规范 | `.editorconfig`（UTF-8/LF/末尾空行） | ✅ 已配置 |
| ESLint 错误数 | **7 个错误**（require() 风格导入） | ⚠️ 需修复 |
| ESLint 警告数 | **100 个警告**（大量未使用变量） | ⚠️ 需清理 |
| TypeScript 编译 | **1 个类型错误**（`scripts/fix-source-type.ts:50`） | ⚠️ 需修复 |

#### 测试覆盖率

| 指标 | 数值 | 评估 |
|------|------|------|
| **总体行覆盖率** | **4.15%** | ⛔ 严重不足 |
| **总体分支覆盖率** | **34.19%** | ⛔ 严重不足 |
| **总体函数覆盖率** | **6.17%** | ⛔ 严重不足 |
| 测试文件数 | 3 个（55 个测试用例） | ⚠️ 覆盖范围极窄 |
| 测试用例通过率 | **100%（55/55）** | ✅ 现有测试全部通过 |

**覆盖率详情**（按模块）：

| 模块 | 行覆盖率 | 评估 |
|------|----------|------|
| `lib/share/codeGenerator.ts` | 100% | ✅ 完整覆盖 |
| `api/share/validate/[code]/route.ts` | 89.3% | ✅ 良好覆盖 |
| `api/share/import/route.ts` | 76.19% | ✅ 良好覆盖 |
| `api/auth/[...nextauth]/route.ts` | 33.6% | ❌ 不足 |
| `lib/apiErrorHandler.ts` | 34% | ❌ 不足 |
| `lib/envValidator.ts` | 27.86% | ❌ 不足 |
| `lib/rateLimit.ts` | 41.66% | ❌ 不足 |
| 其余 50+ 模块 | **0%** | ⛔ 完全未覆盖 |

**未被测试覆盖的关键模块**：
- `middleware.ts`（认证/授权/CSRF 守卫）
- `lib/security.ts`（输入净化/输出验证）
- `lib/csrf.ts`（CSRF 防护）
- `lib/banManager.ts`（封禁管理）
- `lib/injectionDetector.ts`（注入检测）
- `lib/monitoring.ts`（监控服务）
- `services/TranslationService.ts`（545 行核心翻译服务）
- 全部 UI 组件与页面

#### 代码规模
- **源代码文件**：148 个 TS/TSX 文件
- **总代码行数**：约 22,893 行
- **运维脚本**：26 个（数据导入/修复/诊断/测试）

---

## 二、项目可部署性评估

### 2.1 环境配置标准化

**评分：★★★☆☆（中等）**

| 配置项 | 状态 | 评估 |
|--------|------|------|
| `.env.example` | ✅ 已存在 | 包含完整变量说明 |
| `.env.production` | ✅ 已存在 | 生产环境变量模板 |
| `.gitignore` 排除 `.env` | ✅ 已配置 | 敏感信息不入库 |
| 环境变量文档 | `docs/SECRET_MANAGEMENT.md` 完整规范 | ✅ 优秀 |
| 环境变量校验 | 启动时自动校验（`envValidator.ts`） | ✅ 优秀 |
| 变量分类 | 3 必需（`NEXTAUTH_SECRET`/`URL`/`DATABASE_URL`）+ 4 可选 | ✅ 清晰 |

**待改进项**：
- 缺少 `.env.development` 或本地开发快捷配置
- 环境变量校验仅限 Node.js 运行时，Edge Runtime 未覆盖
- 无环境变量模板自动生成工具（如 `setup.sh` 引导脚本）

---

### 2.2 构建流程自动化

**评分：★★☆☆☆（需改进）**

| 能力 | 状态 | 评估 |
|------|------|------|
| 本地构建 | `next build`（Turbopack 编译，4.3 秒） | ✅ 快速 |
| CI/CD 流水线 | **无** GitHub Actions / GitLab CI / Jenkins | ⛔ 缺失 |
| 自动化测试 | **无** CI 中运行测试的配置 | ⛔ 缺失 |
| 自动化部署 | **无**（手动 SSH + PM2 重启） | ⛔ 缺失 |
| 构建产物 | `output: "standalone"` 自包含部署包 | ✅ 支持 |
| 构建成功率 | 本地实测：编译成功，类型检查 1 个非阻塞错误 | 约 95% |

**现状**：当前部署流程完全依赖手动操作——本地构建 → 打包上传 → SSH 登陆 → 替换文件 → PM2 重启。缺乏自动化 CI/CD 流水线是项目可部署性的最大短板。

---

### 2.3 部署脚本完整性

**评分：★★☆☆☆（需改进）**

| 脚本/配置 | 路径 | 功能 | 评估 |
|-----------|------|------|------|
| `start.sh` | 根目录 | 直接 `node server.js` 启动 | ⚠️ 无进程守护 |
| `ecosystem.config.js` | 根目录 | PM2 管理（自动重启/内存限制 1G） | ✅ 生产可用 |
| 构建命令 | `package.json` scripts | `dev`/`build`/`start` | ✅ 标准 |
| 数据库迁移 | Prisma CLI | `prisma migrate deploy` | ⚠️ 无包装脚本 |
| 备份恢复 | `prisma/BACKUP_RESTORE.md` | 手动 cp 命令 | ⚠️ 无自动化 |

**缺失的部署关键项**：
- 无**一键部署脚本**（构建→打包→上传→重启）
- 无**数据库迁移自动化**（当前需手动执行 `prisma migrate deploy`）
- 无**部署前后钩子**（pre-deploy 备份、post-deploy 健康检查）
- 无**环境初始化脚本**（新服务器首次部署步骤繁琐）

---

### 2.4 版本控制策略

**评分：★★★☆☆（中等）**

| 指标 | 现状 | 评估 |
|------|------|------|
| 版本号 | `0.3.0`（语义化版本） | ✅ 已采用 |
| Git 使用 | 活跃提交（10 个最近提交清晰可追溯） | ✅ 已使用 |
| 提交信息 | 中文描述功能/修复变更 | ✅ 可理解 |
| `.gitignore` | 覆盖 node_modules/.next/.env/prisma db 等 | ✅ 完善 |
| 分支策略 | 当前仅 `main` 单分支 | ⚠️ 无开发分支 |
| 标签/Release | **无** Git 标签 | ⛔ 缺失 |

**待改进项**：
- 缺少 Git 标签（Tag）与 Release 管理，无法快速回溯特定版本的部署状态
- 单一 `main` 分支开发，缺少 `develop` / `staging` 多环境分支策略
- 无 CHANGELOG 或自动发版说明

---

### 2.5 回滚机制

**评分：★★☆☆☆（需改进）**

| 回滚层级 | 能力 | 评估 |
|----------|------|------|
| 应用回滚 | 还原旧版 `server.js` + PM2 重启（手动） | ⚠️ 无自动化 |
| 数据库回滚 | Prisma Migrate 支持 `prisma migrate diff` 生成回滚 SQL | ⚠️ 依赖手动 |
| 数据备份 | SQLite 文件简单复制（二进制 + SQL 双格式） | ✅ 基本可用 |
| 备份文档 | `prisma/BACKUP_RESTORE.md` | ✅ 有文档 |

**缺失项**：
- 无**一键回滚脚本**（自动还原上一个部署版本）
- 无**部署前的自动备份触发器**（当前备份为手动操作）
- 无**数据库迁移失败时的自动回滚**
- 备份文件存储在与数据库同目录下（`prisma/`），若磁盘故障则同时丢失
- 缺少异地备份或远程存储策略

---

### 2.6 部署文档清晰度

**评分：★★★☆☆（中等）**

| 文档 | 路径 | 内容质量 |
|------|------|----------|
| 管理员手册 | `docs/ADMIN_GUIDE.md` | ✅ 覆盖权限设置/功能页面/API 接口 |
| 密钥管理规范 | `docs/SECRET_MANAGEMENT.md` | ✅ 完整生命周期管理 |
| 备份恢复文档 | `prisma/BACKUP_RESTORE.md` | ✅ 基本操作说明 |
| 数据库优化指南 | `DATABASE_OPTIMIZATION_GUIDE.md` | ✅ 性能调优参考 |
| 开发路线图 | `ROADMAP.md` | ✅ 986 行详尽规划 |

**缺失项**：
- 无**正式部署文档**（首次部署步骤/环境要求/依赖安装）
- 无**常见故障处理手册**（Troubleshooting Guide）
- 无**系统架构图**（虽存在 `architecture-diagrams.md`，但未确认内容完备性）
- 面向新开发者的**本地开发环境搭建指南**不够完善

---

## 三、主要风险点识别

### 风险分级说明
- 🔴 **严重风险**：可能导致系统不可用、数据丢失或安全漏洞
- 🟡 **中等风险**：影响开发效率、运维可靠性或存在隐患
- 🟢 **低风险**：影响较小，可择机修复

---

### 🔴 严重风险（需立即处理）

| # | 风险描述 | 影响范围 | 当前证据 |
|---|----------|----------|----------|
| R1 | **测试覆盖率仅 4.15%**，关键业务逻辑（翻译服务 545 行、Middleware 认证链、安全模块）完全未测试 | 任何代码变更均无自动化验证 | 覆盖率报告：148 个文件中 50+ 为 0% |
| R2 | **CSP 策略含 `unsafe-inline` 和 `unsafe-eval`**，几乎完全抵消 XSS 防护能力 | 所有页面的 XSS 攻击面 | `next.config.ts` 第 36-37 行 |
| R3 | **缺少 HSTS 头**，不强制 HTTPS | 用户会话可能被降级攻击窃取 | `next.config.ts` 安全头列表中无 HSTS |
| R4 | **CSRF 仅依赖 Origin/Referer 头校验**，无 Token 机制 | 所有非 GET 请求的 CSRF 攻击面 | `src/lib/csrf.ts` |
| R5 | **无 CI/CD 流水线**，部署完全依赖手动操作 | 部署可靠性、一致性无保障 | 无 `.github/workflows/` 或其他 CI 配置 |

---

### 🟡 中等风险（建议尽快处理）

| # | 风险描述 | 影响范围 |
|---|----------|----------|
| R6 | `banManager.ts` 多处静默吞异常，数据库故障时违规记录丢失且调用者无感知 | 安全封禁功能可靠性 |
| R7 | `rateLimit` 和 `requestDeduplication` 默认纯内存实现，多实例下防护失效 | 横向扩展场景 |
| R8 | 无健康检查端点，缺乏优雅关闭处理 | 负载均衡/容器化部署 |
| R9 | 无数据库迁移自动化脚本，部署时可能遗漏迁移 | 生产数据库版本一致性 |
| R10 | `checkCsrfHeader` 遗漏豁免路径检查（与 `validateCsrf` 行为不一致） | `/api/auth` 和 `/api/captcha` 在非标准请求下可能被拦截 |
| R11 | 注入检测纯正则方案，攻击者可通过编码/变体绕过 | LLM 提示词注入风险 |
| R12 | 备份文件存储在 `prisma/` 目录（数据库同级），磁盘故障双双丢失 | 数据灾难恢复 |

---

### 🟢 低风险（可择机处理）

| # | 风险描述 | 影响范围 |
|---|----------|----------|
| R13 | `X-XSS-Protection` 头已过时，现代浏览器中无效且可能引入额外风险 | 旧版浏览器支持 |
| R14 | `validateAiOutput` 不对 `phonetic`/`pos` 字段做注入检测 | LLM 输出辅助字段安全性 |
| R15 | 缺少 Unicode 规范化处理，同形异意字符可能绕过检测 | 字符级注入攻击 |
| R16 | ESLint 100 个 warning（大量未使用变量），TypeScript 1 个类型错误 | 代码质量 |
| R17 | 单一 Git 分支，无 Release 标签管理 | 版本回溯效率 |

---

## 四、改进建议优先级排序

### 优先级 P0（建议 1-2 周内完成）

| # | 改进项 | 目标 | 预计投入 |
|---|--------|------|----------|
| 1 | **配置 CI/CD 流水线**（GitHub Actions）—— 自动运行 lint、type-check、test、build | 每次 push 自动验证代码质量，消除手动部署风险 | 1 天 |
| 2 | **修复安全头配置** —— 添加 HSTS、移除 `unsafe-inline`/`unsafe-eval`（改用 nonce/hash） | 恢复 CSP 的 XSS 防护有效性，强制 HTTPS | 0.5 天 |
| 3 | **实现 CSRF Token 机制** —— 在现有 Origin 校验基础上增加 Double Submit Cookie | 双重 CSRF 防御，消除单一校验点失效风险 | 1 天 |
| 4 | **补充关键模块单元测试** —— 优先覆盖 `middleware.ts`、`security.ts`、`csrf.ts` | 将覆盖率从 4.15% 提升至 20%+ | 3 天 |

### 优先级 P1（建议 1 个月内完成）

| # | 改进项 | 目标 | 预计投入 |
|---|--------|------|----------|
| 5 | **编写部署自动化脚本** —— 涵盖构建、迁移、备份、上传、重启全流程 | 实现一键部署（单命令完成） | 1 天 |
| 6 | **添加健康检查端点**（`/api/health`）—— 返回数据库连接状态等关键指标 | 支持负载均衡器/容器编排的存活检测 | 0.5 天 |
| 7 | **实现优雅关闭** —— 监听 SIGTERM，排空连接后退出 | 零停机部署（滚动更新）前提条件 | 0.5 天 |
| 8 | **修复 `banManager` 异常吞噬** —— 关键路径向上传播错误 | 封禁记录写入失败时不会静默丢失 | 0.5 天 |
| 9 | **创建 `.env.development` 和首次部署指南** | 降低新开发者/新服务器上手指南成本 | 0.5 天 |
| 10 | **修复 ESLint 7 个 error 并清理 100 个 warning** | 消除代码规范的红色告警，统一团队编码风格 | 1 天 |

### 优先级 P2（建议 2-3 个月内完成）

| # | 改进项 | 目标 | 预计投入 |
|---|--------|------|----------|
| 11 | **将 rateLimit 和 requestDeduplication 默认切换至 Redis 后端** | 为多实例水平扩展做好准备 | 1 天 |
| 12 | **补充业务逻辑测试** —— `TranslationService.ts`、`PublicWordService.ts` | 覆盖率提升至 40%+ | 4 天 |
| 13 | **补充 UI 组件测试** —— 关键交互路径（翻译输入/结果展示） | 用户可见功能回归保障 | 3 天 |
| 14 | **数据库备份自动化** —— 定时备份 + 异地存储 | 消除磁盘单点故障导致的永久数据丢失 | 1 天 |
| 15 | **建立 Git 分支规范与 Release 标签管理** | 多环境并行开发 + 版本精准回溯 | 0.5 天 |
| 16 | **主应用 Docker 化** —— 编写 Dockerfile 和 docker-compose.yml | 标准化运行环境，简化多环境部署 | 1 天 |

### 优先级 P3（远期优化）

| # | 改进项 | 目标 |
|---|--------|------|
| 17 | 注入检测引入语义/NLP 辅助判断（替换纯正则方案） | 提升对变体/编码绕过的检测能力 |
| 18 | 实现数据库迁移自动回滚机制 | 迁移失败时自动恢复至迁移前状态 |
| 19 | 添加 E2E 测试（Playwright/Cypress）覆盖核心用户流程 | 端到端用户体验回归保障 |
| 20 | 建设可观测性体系 —— 结构化日志 + 指标采集 + 告警规则 | 生产环境问题快速发现与定位 |
| 21 | NEXTAUTH_SECRET 无缝轮换方案（支持多密钥验证过渡期） | 密钥定期轮换不强制所有用户重新登录 |

---

## 五、总体评估结论

### 综合评分

| 评估维度 | 评分 | 说明 |
|----------|------|------|
| 错误处理机制 | 7.5/10 | 多层纵深防御架构，环境感知降级，部分模块静默吞异常 |
| 边界条件处理 | 6.5/10 | 大部分边界已覆盖，缺 Unicode 规范化 |
| 数据验证 | 7.0/10 | 多层级验证体系，44 个数据库索引，AI 输出验证有遗漏字段 |
| 异常恢复能力 | 6.0/10 | PM2 自动重启，Error Boundary 兜底，缺健康检查与优雅关闭 |
| 代码质量 | 6.0/10 | TypeScript 严格模式，但 ESLint 有 107 个问题，1 个类型错误 |
| **测试覆盖率** | **0.8/10** | **4.15% 行覆盖率，关键业务逻辑完全未测试（最大短板）** |
| 环境配置标准化 | 7.0/10 | 环境变量校验机制优秀，启动时验证 |
| 构建流程自动化 | 3.0/10 | 无 CI/CD，构建依赖手动执行 |
| 部署脚本完整性 | 4.0/10 | PM2 配置可用，缺一键部署/迁移/回滚脚本 |
| 版本控制策略 | 5.5/10 | Git 活跃，缺分支策略和 Release 标签 |
| 回滚机制 | 4.0/10 | 仅基本数据库备份，缺应用回滚脚本 |
| 部署文档清晰度 | 6.0/10 | 有管理员手册和密钥规范，缺正式部署文档 |

### 加权综合评分

\[
\text{健壮性综合评分} = \frac{7.5 + 6.5 + 7.0 + 6.0 + 6.0 + 0.8}{6} \times 10 \approx \mathbf{56.3/100}
\]

\[
\text{可部署性综合评分} = \frac{7.0 + 3.0 + 4.0 + 5.5 + 4.0 + 6.0}{6} \times 10 \approx \mathbf{49.2/100}
\]

### 核心结论

该项目在**功能完备度**和**安全基础设施**方面表现良好——18 个数据模型、多层级安全防护（CSRF/频率限制/注入检测/封禁管理/环境校验）、完善的管理后台和运维脚本，体现了对生产环境的认真考量。

然而，项目在以下两个维度存在显著短板：

1. **测试覆盖率严重不足（4.15%）**：作为最大风险点，关键业务逻辑缺乏自动化测试保障意味着：代码变更后无法快速验证正确性；重构风险极高；回归 Bug 只能在生产环境中被发现。

2. **部署自动化几乎空白**：无 CI/CD 流水线、无 Docker 化主应用、无一键部署/回滚脚本，完全依赖手动操作。这在当前单机单实例阶段尚可维持，但随着项目成长将成为运维效率和可靠性的瓶颈。

**建议优先投入 P0 级改进（约 5.5 人天）**，可将健壮性评分提升至约 **70/100**，可部署性评分提升至约 **65/100**，为项目的持续迭代和大规模使用奠定可靠基础。

---

> **附注**：本报告中所有量化数据（测试覆盖率、ESLint 问题数、代码行数、模型数、索引数等）均基于 2026-04-29 实际运行 `npx vitest --coverage`、`npx eslint .`、`npx next build` 等命令获取的真实数据。
