# 大小写处理功能开发文档

## 1. 功能概述

### 核心目标
实现大小写自动转换机制，确保AI返回的包含特定关键词（如"JavaScript"）的内容能够被自动转换为全小写形式（如"javascript"），以保持系统内部数据的一致性和标准化。

### 应用场景
- **翻译功能**：当用户输入包含特定关键词的文本时，系统需要确保返回的结果中这些关键词的大小写形式符合系统标准
- **数据存储**：确保存储到数据库中的单词数据采用统一的小写形式，便于后续查询和管理
- **用户输入处理**：处理用户输入的不同大小写形式，确保系统能够正确识别和处理

## 2. 实现需求

### 2.1 大小写自动转换机制
- 实现自动检测AI返回结果中的特定关键词（如"JavaScript"）
- 将检测到的关键词转换为全小写形式（如"javascript"）
- 确保转换后的结果保持语义不变

### 2.2 提示词机制设计
- 设计明确的提示词，指导AI输出符合大小写规范的内容
- 在系统提示词中添加大小写处理的相关规则
- 确保提示词清晰、明确，能够被AI正确理解

### 2.3 返回逻辑处理流程
1. **输入检测**：检测用户输入的单词或短语的大小写形式
2. **关键词识别**：识别输入和AI返回结果中的特定关键词
3. **转换规则**：应用大小写转换规则，将特定关键词转换为全小写形式
4. **输出返回**：返回处理后的结果，保持原始输入的大小写形式，同时确保内部处理使用小写形式

## 3. 技术实现细节

### 3.1 大小写转换的具体实现方法

#### 关键词识别与转换
```typescript
// 定义需要特殊处理的关键词列表
const SPECIAL_KEYWORDS = [
  'JavaScript',
  'TypeScript',
  'React',
  'Vue',
  'Angular',
  'Node.js',
  'HTML',
  'CSS',
  'JSON',
  'API',
  'AI',
  'ML',
  'UI',
  'UX'
];

/**
 * 将文本中的特定关键词转换为全小写形式
 * @param text 输入文本
 * @returns 转换后的文本
 */
function convertSpecialKeywordsToLowerCase(text: string): string {
  let result = text;
  
  SPECIAL_KEYWORDS.forEach(keyword => {
    const regex = new RegExp(keyword, 'g');
    result = result.replace(regex, keyword.toLowerCase());
  });
  
  return result;
}
```

#### 单词处理与标准化
```typescript
/**
 * 标准化单词，确保特定关键词为小写形式
 * @param word 输入单词
 * @returns 标准化后的单词
 */
function normalizeWord(word: string): string {
  // 首先检查是否为特殊关键词
  for (const keyword of SPECIAL_KEYWORDS) {
    if (word.toLowerCase() === keyword.toLowerCase()) {
      return keyword.toLowerCase();
    }
  }
  
  // 对于非特殊关键词，保持原始大小写
  return word;
}
```

### 3.2 提示词设计原则与内容

#### 提示词设计原则
1. **明确性**：明确告知AI如何处理大小写
2. **一致性**：确保AI输出的大小写形式与系统标准一致
3. **准确性**：确保AI能够正确理解和执行大小写处理规则

#### 提示词具体内容
在现有的系统提示词中添加以下内容：

```
【大小写处理规则】
1. 对于特定技术术语和专有名词（如JavaScript、TypeScript、React等），请在输出中使用全小写形式
2. 对于首字母缩写词（如API、AI、ML等），请保持其原始大小写形式
3. 对于普通单词，请保持其原始大小写形式

【示例】
- 输入: "JavaScript"
- 输出: "javascript"

- 输入: "API"
- 输出: "API"

- 输入: "Apple"
- 输出: "Apple"
```

### 3.3 边界情况和异常输入处理

#### 边界情况处理
1. **混合大小写**：处理如"JaVaScRiPt"这样的混合大小写输入
2. **部分匹配**：确保只匹配完整的关键词，避免部分匹配导致错误转换
3. **上下文相关**：考虑关键词在不同上下文中的大小写要求

#### 异常输入处理
1. **空输入**：处理空字符串或null值
2. **特殊字符**：处理包含特殊字符的输入
3. **超长输入**：处理长度超出预期的输入

```typescript
/**
 * 处理边界情况和异常输入
 * @param word 输入单词
 * @returns 处理后的单词
 */
function handleEdgeCases(word: string): string {
  // 处理空输入
  if (!word || word.trim() === '') {
    return '';
  }
  
  // 处理混合大小写
  const normalizedWord = word.trim();
  
  // 检查是否为特殊关键词
  for (const keyword of SPECIAL_KEYWORDS) {
    if (normalizedWord.toLowerCase() === keyword.toLowerCase()) {
      return keyword.toLowerCase();
    }
  }
  
  // 对于非特殊关键词，保持原始大小写
  return normalizedWord;
}
```

## 4. 测试要求

### 4.1 测试用例

#### 正常情况
| 输入 | 预期输出 | 说明 |
|------|----------|------|
| "JavaScript" | "javascript" | 特殊关键词转换为小写 |
| "TypeScript" | "typescript" | 特殊关键词转换为小写 |
| "React" | "react" | 特殊关键词转换为小写 |
| "API" | "API" | 首字母缩写词保持原样 |
| "AI" | "AI" | 首字母缩写词保持原样 |
| "Apple" | "Apple" | 普通单词保持原样 |
| "banana" | "banana" | 普通单词保持原样 |

#### 边界情况
| 输入 | 预期输出 | 说明 |
|------|----------|------|
| "JaVaScRiPt" | "javascript" | 混合大小写转换为小写 |
| "JAVA" | "java" | 全大写特殊关键词转换为小写 |
| "api" | "api" | 全小写首字母缩写词保持原样 |
| "" | "" | 空字符串处理 |
| "  JavaScript  " | "javascript" | 带空格的输入处理 |

### 4.2 测试方法
1. **单元测试**：编写单元测试，测试各个函数的功能
2. **集成测试**：测试整个处理流程
3. **边界测试**：测试边界情况和异常输入
4. **回归测试**：确保修改不会影响现有功能

### 4.3 预期结果
- 所有测试用例通过
- 特殊关键词能够正确转换为小写形式
- 首字母缩写词和普通单词保持原样
- 系统能够处理各种边界情况和异常输入

## 5. 注意事项

1. **范围限制**：当前版本仅聚焦于大小写处理功能，其他功能暂不做调整
2. **核心目标**：以实现核心需求为首要目标，现阶段不追求功能的完美性
3. **兼容性**：确保修改与现有代码和架构兼容
4. **性能**：确保大小写处理不会显著影响系统性能
5. **可维护性**：确保代码结构清晰，易于维护和扩展

## 6. 实现计划

### 6.1 阶段一：准备工作
1. 分析现有代码结构和架构
2. 确定需要修改的文件和函数
3. 设计大小写处理的具体实现方案

### 6.2 阶段二：核心实现
1. 实现关键词识别与转换功能
2. 修改系统提示词，添加大小写处理规则
3. 集成大小写处理功能到现有代码中

### 6.3 阶段三：测试与验证
1. 编写测试用例
2. 执行测试，验证功能正确性
3. 修复测试中发现的问题

### 6.4 阶段四：部署与监控
1. 部署修改后的代码
2. 监控系统运行情况
3. 收集用户反馈，持续优化

## 7. 代码集成点

### 7.1 输入处理阶段
在`src/app/api/translate/route.ts`文件中，修改输入处理逻辑，添加大小写处理功能：

```typescript
// 在sanitizeWordList和normalizedWords处理后添加
const sanitizedWords = sanitizeWordList(words);
const normalizedWords = sanitizedWords.map((w: string) => {
  // 应用大小写处理
  return normalizeWord(w.toLowerCase().trim());
}).filter(Boolean);
```

### 7.2 AI结果处理阶段
在处理AI返回结果时，添加大小写处理：

```typescript
// 在解析AI结果后添加
aiParsedResults = parsed.results.map((result: any) => ({
  ...result,
  word: normalizeWord(inputWordMap.get((Array.isArray(result.word) ? result.word[0] : result.word).toLowerCase()) || (Array.isArray(result.word) ? result.word[0] : result.word))
}));
```

### 7.3 数据库存储阶段
在存储到数据库前，确保使用标准化的单词形式：

```typescript
const wordsToSave = aiParsedResults.filter((item: any) => 
  item.pos !== "错误" && 
  item.pos !== "风控" &&
  item.pos !== "中断" &&
  item.pos !== "非英语" &&
  item.pos !== "句子" &&
  !(item.translation && item.translation.includes("拼写错误或不存在")) &&
  !(item.translation && item.translation.includes("粗俗或敏感")) &&
  !(item.translation && item.translation.includes("⚠️"))
).map((item: any) => ({
  word: normalizeWord(String(item.word || '').toLowerCase().trim()),
  phonetic: item.phonetic || null,
  pos: item.pos || null,
  translation: item.translation || '',
  example: item.example || null,
  exampleTranslation: item.exampleTranslation || null,
})).filter((w: any) => w.word);
```

## 8. 总结

大小写处理功能是确保系统数据一致性和标准化的重要组成部分。通过实现自动转换机制、设计明确的提示词和优化返回逻辑，可以确保系统能够正确处理各种大小写形式的输入和输出，提升用户体验和系统可靠性。

本开发文档提供了详细的实现方案和测试要求，旨在指导开发人员高效、准确地实现大小写处理功能，同时确保与现有代码和架构的兼容性。